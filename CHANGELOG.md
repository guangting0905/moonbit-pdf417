# Changelog

本项目遵循「一个版本一个可验证的变更」的原则记录，不设严格语义化版本号——
它还未发布到 mooncakes.io，先按日期记录。

## 2026-09-25 · 首个完整版本

从零建立 PDF417 编解码器。范围与边界都在 README 里写明，这里按层记录。

### 编码

- **三种压缩模式**：文本（base 30，两字符一码字，含四个子模式与切换码）、
  字节（6 字节 → 5 码字，base 256 → base 900）、数字（44 位数字 → 15 码字）。
- **分段决策**：与参考实现一致的贪心策略，使码字流可以逐条比对；
  短于 13 位且紧邻文本的数字串会退回文本模式（否则多付一个锁存码）。
- **Reed-Solomon over GF(929)**：生成多项式 `g(x) = ∏(x − 3^i)` **现算**，
  不查表；与 ISO/IEC 15438 的系数表 9 个级别逐项一致。
- **布局**：长度描述符、填充码字、每行两个行指示符、簇号 0/3/6 循环，
  以及起始／终止图案。

### 解码

- **符号字符图案匹配**：精确匹配优先，失配时取 17 位汉明距离最近的图案，
  并单独计数（`unmatched_characters`）。
- **行指示符多数表决**：逐行收集、精确匹配才计票，取众数——
  而不是信任第一个能读到的值。
- **纠错**：伴随式 → Berlekamp-Massey → Chien 搜索 → Forney，
  超出容量时明确报错而不猜。
- **逆压缩**：三种模式的边界规则，其中字节段以「码字 ≥ 900」终止，
  长度描述符在纠错之后读取。

### 渲染

- `to_text`（终端）、`to_pbm`（P1，一行一样本）、`to_svg`（暗模块按水平游程合并）。
- 不出 PNG／JPEG：位图栅格化属于图像库职责，输出 PBM 后一行 shell 即可接上。

### 浏览器与命令行

- `cmd/main`：`encode` / `decode` / `info` / `selfcheck`，参数进、stdout 出，
  不读文件、不用 FFI，因此 `moon build --target all` 不需要 C 编译器之外的依赖。
- `cmd/web` + `web/index.html`：把编解码暴露给浏览器的在线试用页。
  跨 FFI 边界只传字符串，bundle 是普通脚本，页面一个 `<script src>` 即可。
  `scripts/check_web.mjs` 用 Node 加两个桩全局变量对该产物做冒烟测试。

### 验收证据

| 层 | 结果 |
| --- | --- |
| 与参考实现的一致性向量 | 127 / 127 |
| 单元测试 | 202 / 202，零警告 |
| 纠错系数（9 级，1022 项） | 与标准表逐项一致 |
| 符号字符图案表结构自检 | 2787 / 2787 |
| 单码字错误穷举（312 组） | 全部修复 |
| 容量边界（注满 8 / 16 个错误） | 全部修复 |
| 随机载荷往返 | 110 条全过，40 条被几何规则拒绝，0 条错误 |
| 模块级错误注入 | 23 / 23 恢复 |
| zxing-cpp 解码 | 23 / 23 |

### 过程中修掉的两个静默错误

1. **长度描述符在纠错之前被读取**，导致该码字损坏时载荷被截断，
   而报告仍显示「错误已全部修正」。
2. **字节压缩解码用 32 位累加器**，`900^5 ≈ 5.9e14` 溢出，
   表现为载荷从第 7 个字节起整体偏移。

两处都由错误注入/往返测试抓出，修复后各补了具名回归用例。

### 工具链与兼容性

- `cmd/main` 声明 `supported_targets = "js"`（需要 `process.argv`），
  因此 `moon build --target all` 在没有 C 编译器的机器上也能通过；
  库本体 `src/pdf417` 与后端无关，在 wasm 目标上跑全部测试。
- 生成物由脚本产出并带自检：`scripts/gen_codeword_table.py`、
  `scripts/gen_vectors.py`。
