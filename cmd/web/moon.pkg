import {
  "moonbitlang/core/string",
  "guangting0905/moonbit-pdf417/src/pdf417",
}

pkgtype(kind: "executable")

// Browser entry point: publishes `globalThis.pdf417` for web/index.html.
// Restricted to `js` because that is the only backend with a `globalThis`.
supported_targets = "js"
